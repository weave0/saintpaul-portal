# Example environment variables (DO NOT COMMIT REAL SECRETS)
API_PUBLIC_KEY=xatrujvd
API_PRIVATE_KEY=REPLACE_WITH_YOUR_PRIVATE_KEY

# MongoDB Atlas (choose either full URI or components below)
# Option A: direct connection string (prefer referencing components instead of embedding password)
# MONGODB_URI=mongodb+srv://brettlweaver_db_user:REPLACE_PASSWORD@cluster0.example.mongodb.net/saintpaul?retryWrites=true&w=majority

# Option B: components (recommended)
MONGODB_USERNAME=brettlweaver_db_user
MONGODB_PASSWORD=REPLACE_PASSWORD
MONGODB_HOST=cluster0.example.mongodb.net
MONGODB_DBNAME=saintpaul
MONGODB_POOL_SIZE=10

# Frontend configurable base API URL (optional; default is relative)
VITE_API_BASE_URL=http://localhost:5000
VITE_ENABLE_PERF=false
VITE_MAPBOX_TOKEN=REPLACE_WITH_MAPBOX_TOKEN

# To use, copy to .env and fill in values:
# cp .env.example .env