name = "saintpaul-portal"
compatibility_date = "2024-11-24"

# Pages configuration
pages_build_output_dir = "dist"

[env.production]
# Production environment variables
vars = { }

# You'll need to set secrets via Cloudflare dashboard:
# - VITE_MAPBOX_TOKEN
# - VITE_API_BASE_URL (when backend is deployed)
